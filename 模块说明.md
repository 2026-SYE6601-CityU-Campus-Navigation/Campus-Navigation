# 校园导航 · 本地 AI 模块（AI-Guidance）

香港城市大学 SYE6601「Campus Navigation」项目的 AI 路引模块——本地生成式 AI 部署、轨迹数据分析、向量检索问答与「探照灯」导航图路引。全部代码仅依赖 Python 标准库 + 本地 Ollama，无需联网即可运行。

## 核心概念

**探照灯导航图**：每一条轨迹像探照灯，照亮建筑内部一小段结构。用户打的标签（门/电梯/门禁）是图的节点，相邻标签之间的轨迹段是图的边。路引规划只沿「已探索的边」行走——没有被轨迹覆盖的区域视为未探索，绝不把两点凭空直线相连（如同高德导航只用真实道路）。多录一条轨迹，图就扩大一圈。

**三级降级**（AI 路引模块总体架构）：

1. 本地推理（Ollama + qwen3:8b）——完全离线
2. 本地向量 RAG（qwen3-embedding:0.6b 检索 + 轨迹知识库）——用已有记录约束回答、防幻觉
3. 云端大模型（可选）——演示/复杂场景兜底

**数据原则**：

- 方向永远用「直行/稍向左转/稍向右转/左转/右转/掉头」，不输出角度数值
- 只依据轨迹记录生成；无备注的标签不会被猜测合并（数据标注质量决定图质量）
- 气压计是垂直定位最可靠信号（实测电梯落差 14~15 米被精确记录；室内 GPS 高度失效）

## 目录结构

```
AI-Guidance/
├── LocalAI/
│   ├── install_ollama.bat / .ps1   一键部署：Ollama 装 D 盘，按显存自动选模型
│   ├── fix_models_to_d.bat / .ps1  模型目录搬到 D 盘（环境变量时序修复）
│   ├── describe_trajectory.py      轨迹 → 统计特征 → 中文轨迹描述
│   ├── generate_guidance.py        轨迹 → 节点路引（支持 --reverse 反向）
│   ├── rag_guide.py                向量 RAG：轨迹自动分块建库 + 检索增强问答
│   ├── nav_graph.py                探照灯导航图：只沿已探索路径规划路引
│   ├── test_api.py                 Ollama API 调用示例
│   └── 使用说明.md                  本地部署与使用文档
├── TrajectoryData/
│   └── 数据格式说明.md              轨迹数据 JSON Lines 格式契约（三端共用）
└── 模块说明.md
```

## 快速开始

```bash
# 1. 一键部署本地 AI（Ollama + 按显存选模型，装到 D 盘）
LocalAI\install_ollama.bat

# 2. 拉取向量检索模型
D:\Ollama\ollama.exe pull qwen3-embedding:0.6b

# 3. 建立轨迹知识库（扫描轨迹目录自动分块+向量化）
python LocalAI\rag_guide.py --build

# 4. 生成路引（默认：小区大门 → 四楼目标房间；可指定任意已探索节点）
python LocalAI\nav_graph.py "大门" "410"
python LocalAI\nav_graph.py --llm "大门" "410"      # 大模型润色版
python LocalAI\nav_graph.py --graph                 # 查看已探索的导航图
```

## 示例输出（已脱敏）

```
【导航路引】某小区大门 → 四楼目标房间（共 4 段，全程约 128 秒）
第 1 段 · 大门 → 门禁：直行约 31 秒
第 2 段 · 门禁 → 一楼电梯：直行约 6 秒
第 3 段 · 一楼电梯 → 四楼电梯：乘电梯约 65 秒，上升约 14 米
第 4 段 · 四楼电梯 → 目标房间：掉头（向左后方）约 27 秒
本路引仅依据已探索路径生成，未经轨迹记录的岔路不在其中。
```

## 轨迹数据格式

见 `TrajectoryData/数据格式说明.md`。要点：JSON Lines，每行一个轨迹点，字段含时间戳、经纬度、楼层、气压、航向、步数、室内外模式、锚点等；三端 App 按平台子文件夹（Harmony/iOS/Android）追加写入。

## 已知限制与下一步

- 室内 GPS 基本不可用：位移统计依赖标签与气压；电梯段含等梯时间，段时长偏大
- 无备注标签不会并入导航图：录轨迹时标签写备注、名称保持一致，图即可跨轨迹连通
- 待办：三端 App 接入统一写入；RAG 接入路引模块本地层；更多校园轨迹扩展知识库
