# 开发日志 2026-09-16（RoomMarkerHarmony 功能扩展）

> 本条目由开发会话生成，请追加到项目根目录 `开发日志.md`（本会话挂载目录未包含项目根目录）。

## 日期
2026-09-16

## 目标
RoomMarkerHarmony 增加 6 项功能：①区域（文件夹）管理；②区域数据批量导出；③开始轨迹前强制确认区域；
④轨迹途中拍照并与数据关联（供 AI 读取）；⑤采样点增加手机朝向；⑥途中标签（厕所/楼梯/门/门禁/电梯/教室）+ 备注。

## 决策
- **区域归属范围**（用户确认）：房间与轨迹都归入区域。首页改为区域列表，进入区域看到该区的房间+轨迹；
  升级前旧数据自动归入「未分区」入口（areaId 为 NULL，UI 约定 id=0）。「未分区」可看可导出但不可开始记录。
- **导出格式**（用户确认）：ZIP 压缩包。每条轨迹一个 JSON（采样点+标签+照片元数据）+ 照片原图 + manifest.json，
  通过 DocumentViewPicker.save 保存。因 HarmonyOS 无官方多文件 zip API，手写 ZipWriter（STORED 不压缩 + CRC32 + UTF-8 文件名），
  已在 Linux 沙箱用 Node 复现算法并通过 `unzip -t` / Python zipfile CRC 校验、字节级比对。
- **朝向**：实时页原本有加速度+地磁融合方位角但轨迹数据未入库，不算重复 → 新增。TrackRecorder 增加加速度计订阅，
  融合方位角（GPS direction 兜底）写入 track_points.headingDeg；标签/照片同样关联朝向。融合逻辑提取为 Utils.fusedAzimuth，LiveStream 同步复用。
- **拍照**：cameraPicker.pick（免相机权限），结果 URI 逐字节复制到 filesDir/photos/{trackId}/{ts}.jpg（防临时授权过期），
  落库相对路径 + 时间 + 定位 + 朝向 + 备注；用户丢弃则删文件。拍摄期间若停止记录则自动丢弃照片。
- **删除区域三选**：取消 / 移入未分区（不丢数据）/ 彻底删除（级联删房间+标记、轨迹+采样点+标签+照片文件）；
  彻底删除前若正在记录会先 stop 收尾。
- **刷新方案**：延续 API 21 已验证的「@State heartbeat 500ms + ForEach 重建」模式（RecordingBanner 及页面 FAB），
  不依赖 @StorageLink（NavDestination 下不生效）。
- **数据库迁移**：新增 areas / track_tags / track_photos 三表；rooms/tracks 加 areaId、track_points 加 headingDeg，
  用 try-ALTER-TABLE 兼容旧库。trajectory 标签与房间 MarkerType 是两套体系，独立存储。

## 产出
- 数据层：Entities.ets（Area/TrackTag/TrackPhoto/TrackTagType + Track.areaId/areaName + TrackPoint.headingDeg）；
  Store.ets（区域 CRUD 含计数、按区域查询、级联删除、标签/照片 CRUD、照片文件清理）
- 工具：Utils.fusedAzimuth/compassDir/safeFileName；ZipWriter.ets；Exporter.ets
- 传感器：TrackRecorder（start(name, areaId)、朝向采样、getTrackId/getSnapshot）；TrackMedia.ets（拍照+复制沙箱）
- 页面：Index 区域列表首页；AreaDetail（新建，含导出/删除区域/开始记录）；TrackList（StartTrackDialog 强制选区域、
  区域名展示）；TrackDetail（标签/照片区块、缩略图点开大图、朝向显示）；RecordingBanner（拍照/打标签/停止，共享组件）；
  CaptureDialogs（标签选择+照片备注）
- README.md 更新功能/结构/实现说明

## 验证
- ZIP 打包器算法在沙箱 Node 复现验证：unzip -t 全部 OK、Python zipfile CRC 校验通过、中文文件名与照片字节一致。
- 静态自查：无解构赋值、无 any、无旧签名残留调用；修正 AreaDetail 对话框在成员初始化时读取 this.areaId 的
  ArkUI 陷阱（改为点击时动态构造 controller）。
- ⚠️ 尚未在 DevEco 编译/真机运行（本会话环境无法构建），下一步需用户在 IDE Sync + Build 验证。

## 下一步
1. DevEco 6.0.1 打开工程 Sync + Build，编译报错按 API 21 编译器提示修正（重点风险点：
   cameraPicker 命名、@kit.CoreFileKit 的 fileIo/picker 导出、DocumentSaveOptions 构造参数）。
2. 真机验证：新建区域→开始记录→途中拍照/打标签→停止→批量导出 zip 解压核对；旧库升级后旧数据出现在「未分区」。
3. 后续可选：房间/轨迹跨区域移动、照片 EXIF 写入、ZIP 压缩率优化、标签与照片互相关联。

## 风险
- cameraPicker/DocumentViewPicker 的具体 API 形状以 SDK 编译器为准，若报错需微调（低风险，均有官方 API 12 文档依据）。
- 手写 ZIP 在极端大文件（>4GB）不适用；课程项目数据量无虞。
- 照片存储于应用沙箱，卸载应用会丢照片；批量导出是唯一外部备份途径，建议采集后及时导出。
